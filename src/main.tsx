import React from 'react'
import ReactDOM from 'react-dom/client'
import './index.css'

import CashcodeLanding from "./CashcodeLanding.js"

	ReactDOM.createRoot(document.getElementById("root")!).render(
		<React.StrictMode>
			<CashcodeLanding />
		 </React.StrictMode>
	);
